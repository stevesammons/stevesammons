{
  "name": "Quartet songs: chord clips",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "field": "minutes",
              "minutesInterval": 5
            }
          ]
        }
      },
      "id": "every-5-minutes",
      "name": "Every 5 minutes",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        0,
        300
      ]
    },
    {
      "parameters": {
        "assignments": {
          "assignments": [
            {
              "id": "supabase-url",
              "name": "supabase_url",
              "type": "string",
              "value": "https://YOUR-PROJECT-REF.supabase.co"
            },
            {
              "id": "notify-email",
              "name": "notify_email",
              "type": "string",
              "value": "you@example.com"
            }
          ]
        },
        "options": {}
      },
      "id": "settings",
      "name": "Settings",
      "type": "n8n-nodes-base.set",
      "typeVersion": 3.4,
      "position": [
        220,
        300
      ]
    },
    {
      "parameters": {
        "operation": "getAll",
        "tableId": "songs",
        "limit": 5,
        "filterType": "manual",
        "matchType": "allFilters",
        "filters": {
          "conditions": [
            {
              "keyName": "status",
              "condition": "eq",
              "keyValue": "ready"
            }
          ]
        }
      },
      "id": "songs-ready-for-clips",
      "name": "Songs ready for clips",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        440,
        300
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForAllItems",
        "language": "javaScript",
        "jsCode": "// Renders a song's chord chart into piano-and-bass WAV clips for Suno's Audio reference.\n// JavaScript twin of songs/render_chords.py (same chart format, same sound), so it can run\n// inside an n8n Code node with nothing installed. build_workflow.py pastes this file into\n// the workflow; test it with: node songs/n8n/render-clips.js songs/dinah/chords.json\n\nconst RATE = 22050;\nconst NOTE = { C: 0, D: 2, E: 4, F: 5, G: 7, A: 9, B: 11 };\nconst QUALITY = {\n  '': [0, 4, 7], m: [0, 3, 7], '7': [0, 4, 7, 10], m7: [0, 3, 7, 10],\n  maj7: [0, 4, 7, 11], '6': [0, 4, 7, 9], m6: [0, 3, 7, 9],\n  dim: [0, 3, 6], dim7: [0, 3, 6, 9], sus4: [0, 5, 7], '7sus4': [0, 5, 7, 10],\n  '9': [0, 4, 7, 10, 14], add9: [0, 4, 7, 14],\n};\nconst CHORD = /^([A-G])([#b]?)([^/~]*)(?:\\/([A-G][#b]?))?(?:~(\\d+))?$/;\n\nfunction pitchClass(name) {\n  const shift = name[1] === '#' ? 1 : name[1] === 'b' ? -1 : 0;\n  return (((NOTE[name[0]] + shift) % 12) + 12) % 12;\n}\n\nconst mod12 = (n) => ((n % 12) + 12) % 12;\n\nfunction parseChord(symbol) {\n  const m = CHORD.exec(symbol);\n  if (!m || !(m[3] in QUALITY)) throw new Error(`Unknown chord: ${symbol}`);\n  const root = pitchClass(m[1] + m[2]);\n  return {\n    root,\n    intervals: QUALITY[m[3]],\n    bass: m[4] ? pitchClass(m[4]) : root,\n    hold: parseInt(m[5] || '1', 10),\n  };\n}\n\nfunction chordEvents(chart, sectionNames) {\n  const bpb = chart.beats_per_bar;\n  const out = [];\n  let beat = 0;\n  for (const name of sectionNames) {\n    const section = chart.sections[name];\n    if (!section) throw new Error(`No section named ${name}`);\n    const feel = section.feel || 'pattern';\n    for (const bar of section.bars) {\n      const chords = bar.trim().split(/\\s+/);\n      let split = chords.map(() => bpb / chords.length);\n      if (chords.length === 2 && bpb === 3) split = [2, 1];\n      chords.forEach((symbol, i) => {\n        const c = parseChord(symbol);\n        const bassNote = 40 + mod12(c.bass - 40);\n        const tones = c.intervals.map((iv) => 55 + mod12(c.root + iv - 55)).sort((a, b) => a - b);\n        const total = split[i] + (c.hold - 1) * bpb;\n        out.push({ start: beat, beats: total, bassNote, tones, feel });\n        beat += total;\n      });\n    }\n  }\n  return out;\n}\n\nfunction renderClip(chart, sectionNames) {\n  const spb = 60 / chart.bpm;\n  const evs = chordEvents(chart, sectionNames);\n  const last = evs[evs.length - 1];\n  const totalSeconds = (last.start + last.beats) * spb + 2;\n  const audio = new Float64Array(Math.floor(totalSeconds * RATE) + RATE);\n\n  const add = (startBeat, beats, note, vol, decay, bright) => {\n    const start = Math.floor(startBeat * spb * RATE);\n    const seconds = beats * spb + 0.3;\n    const n = Math.floor(seconds * RATE);\n    const freq = 440 * Math.pow(2, (note - 69) / 12);\n    for (let i = 0; i < n && start + i < audio.length; i++) {\n      const t = i / RATE;\n      let w = 0;\n      for (let k = 0; k < 5; k++) w += Math.pow(bright, k) * Math.sin(2 * Math.PI * freq * (k + 1) * t);\n      const env = Math.exp(-t * decay) * Math.min(1, t / 0.005);\n      const tail = Math.min(1, (seconds - t) / 0.05);\n      audio[start + i] += vol * w * env * tail;\n    }\n  };\n\n  for (const e of evs) {\n    if (e.feel === 'sustain') {\n      add(e.start, e.beats, e.bassNote, 0.30, 0.35, 0.35);\n      for (const n of e.tones) add(e.start, e.beats, n, 0.13, 0.30, 0.45);\n      continue;\n    }\n    add(e.start, e.beats, e.bassNote, 0.32, 1.2, 0.35); // bass on the downbeat\n    for (let off = 1; off < e.beats; off += 1) {        // chord on the remaining beats\n      for (const n of e.tones) add(e.start + off, Math.min(1, e.beats - off), n, 0.11, 2.5, 0.5);\n    }\n  }\n\n  let peak = 0;\n  for (const v of audio) peak = Math.max(peak, Math.abs(v));\n  const scale = (peak || 1) / 0.8;\n  const wav = Buffer.alloc(44 + audio.length * 2);\n  wav.write('RIFF', 0);\n  wav.writeUInt32LE(36 + audio.length * 2, 4);\n  wav.write('WAVEfmt ', 8);\n  wav.writeUInt32LE(16, 16);\n  wav.writeUInt16LE(1, 20);        // PCM\n  wav.writeUInt16LE(1, 22);        // mono\n  wav.writeUInt32LE(RATE, 24);\n  wav.writeUInt32LE(RATE * 2, 28);\n  wav.writeUInt16LE(2, 32);\n  wav.writeUInt16LE(16, 34);\n  wav.write('data', 36);\n  wav.writeUInt32LE(audio.length * 2, 40);\n  for (let i = 0; i < audio.length; i++) wav.writeInt16LE(Math.trunc((audio[i] / scale) * 32767), 44 + i * 2);\n  return { wav, seconds: totalSeconds };\n}\n\n// Returns { clipName: { wav: Buffer, seconds } } for every clip the chart lists.\nfunction renderClips(chart) {\n  const clips = {};\n  for (const [name, sections] of Object.entries(chart.clips)) clips[name] = renderClip(chart, sections);\n  return clips;\n}\n\n// ---- n8n: one output item per song row, with the clips as binary 'reference' and 'tag' ----\nconst out = [];\nfor (const item of $input.all()) {\n  const row = item.json;\n  const base = {\n    post_id: row.post_id, post_title: row.post_title, character: row.character,\n    song_title: row.song_title, style: row.style, exclude_styles: row.exclude_styles,\n    lyrics: row.lyrics, notes: row.notes,\n  };\n  try {\n    const chart = typeof row.chords === 'string' ? JSON.parse(row.chords) : row.chords;\n    if (!chart || !chart.clips || !chart.clips.reference || !chart.clips.tag) {\n      throw new Error('chords must list clips.reference and clips.tag');\n    }\n    const clips = renderClips(chart);\n    const slug = String(row.song_title || row.post_id).toLowerCase()\n      .replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');\n    const binary = {};\n    for (const [name, clip] of Object.entries(clips)) {\n      binary[name] = {\n        data: clip.wav.toString('base64'), mimeType: 'audio/wav',\n        fileName: `${slug}-chords-${name}.wav`, fileExtension: 'wav',\n      };\n    }\n    out.push({\n      json: {\n        ...base, ok: true,\n        reference_clip: `${row.post_id}/chords-reference.wav`,\n        tag_clip: `${row.post_id}/chords-tag.wav`,\n        reference_seconds: Math.round(clips.reference.seconds),\n      },\n      binary,\n    });\n  } catch (e) {\n    out.push({ json: { ...base, ok: false, error: String((e && e.message) || e) } });\n  }\n}\nreturn out;\n"
      },
      "id": "render-chord-clips",
      "name": "Render chord clips",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        660,
        300
      ]
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "loose"
          },
          "conditions": [
            {
              "id": "ok",
              "leftValue": "={{ $json.ok }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "true",
                "singleValue": true
              }
            }
          ],
          "combinator": "and"
        },
        "options": {}
      },
      "id": "rendered-ok?",
      "name": "Rendered OK?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [
        880,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Settings').first().json.supabase_url }}/storage/v1/object/song-clips/{{ $json.reference_clip }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-upsert",
              "value": "true"
            },
            {
              "name": "Content-Type",
              "value": "audio/wav"
            }
          ]
        },
        "sendBody": true,
        "contentType": "binaryData",
        "inputDataFieldName": "reference",
        "options": {}
      },
      "id": "upload-reference-clip",
      "name": "Upload reference clip",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1120,
        60
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $('Settings').first().json.supabase_url }}/storage/v1/object/song-clips/{{ $json.tag_clip }}",
        "authentication": "predefinedCredentialType",
        "nodeCredentialType": "supabaseApi",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-upsert",
              "value": "true"
            },
            {
              "name": "Content-Type",
              "value": "audio/wav"
            }
          ]
        },
        "sendBody": true,
        "contentType": "binaryData",
        "inputDataFieldName": "tag",
        "options": {}
      },
      "id": "upload-tag-clip",
      "name": "Upload tag clip",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1120,
        220
      ]
    },
    {
      "parameters": {
        "sendTo": "={{ $('Settings').first().json.notify_email }}",
        "subject": "=Song ready for Suno: {{ $json.character }} ({{ $json.song_title }})",
        "emailType": "text",
        "message": "={{ $json.character }}: {{ $json.song_title }}\nPost {{ $json.post_id }}: {{ $json.post_title }}\n\nIN SUNO: Create > Advanced, model v6\n1. Audio: upload the attached {{ $binary.reference.fileName }} as the audio reference (not Cover).\n2. More Options: Vocal Gender Male, Audio Influence about 40%, Style Influence about 75%, Weirdness about 25%.\n3. Exclude styles: {{ $json.exclude_styles }}\n4. Title: {{ $json.song_title }}\n5. Styles box:\n\n{{ $json.style }}\n\n6. Lyrics box:\n\n{{ $json.lyrics }}\n\nNOTES\n{{ $json.notes }}\n\nIf the ending falls flat, use the Song Editor's Replace Section on the last 20-30 seconds with the [Tag] and [End] lines; the tag clip is attached too.\nWhen you've generated it, set the row's status to 'generated' and paste the two links into candidate_urls.",
        "options": {
          "appendAttribution": false,
          "attachmentsUi": {
            "attachmentsBinary": [
              {
                "property": "reference,tag"
              }
            ]
          }
        }
      },
      "id": "email-the-song",
      "name": "Email the song",
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.1,
      "position": [
        1120,
        380
      ]
    },
    {
      "parameters": {
        "operation": "update",
        "tableId": "songs",
        "filterType": "manual",
        "matchType": "allFilters",
        "filters": {
          "conditions": [
            {
              "keyName": "post_id",
              "condition": "eq",
              "keyValue": "={{ $('Render chord clips').item.json.post_id }}"
            }
          ]
        },
        "dataToSend": "defineBelow",
        "fieldsUi": {
          "fieldValues": [
            {
              "fieldId": "status",
              "fieldValue": "clips_ready"
            },
            {
              "fieldId": "reference_clip",
              "fieldValue": "={{ $('Render chord clips').item.json.reference_clip }}"
            },
            {
              "fieldId": "tag_clip",
              "fieldValue": "={{ $('Render chord clips').item.json.tag_clip }}"
            },
            {
              "fieldId": "clips_made_at",
              "fieldValue": "={{ $now.toISO() }}"
            },
            {
              "fieldId": "clip_error",
              "fieldValue": ""
            }
          ]
        }
      },
      "id": "mark-clips-ready",
      "name": "Mark clips ready",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        1340,
        380
      ]
    },
    {
      "parameters": {
        "operation": "update",
        "tableId": "songs",
        "filterType": "manual",
        "matchType": "allFilters",
        "filters": {
          "conditions": [
            {
              "keyName": "post_id",
              "condition": "eq",
              "keyValue": "={{ $json.post_id }}"
            }
          ]
        },
        "dataToSend": "defineBelow",
        "fieldsUi": {
          "fieldValues": [
            {
              "fieldId": "status",
              "fieldValue": "needs_fix"
            },
            {
              "fieldId": "clip_error",
              "fieldValue": "={{ $json.error }}"
            }
          ]
        }
      },
      "id": "mark-needs-fix",
      "name": "Mark needs fix",
      "type": "n8n-nodes-base.supabase",
      "typeVersion": 1,
      "position": [
        1120,
        600
      ]
    },
    {
      "parameters": {
        "sendTo": "={{ $('Settings').first().json.notify_email }}",
        "subject": "=Chord clips failed: post {{ $('Rendered OK?').item.json.post_id }}",
        "emailType": "text",
        "message": "=n8n couldn't render the chord clips for post {{ $('Rendered OK?').item.json.post_id }} ({{ $('Rendered OK?').item.json.song_title }}).\n\nError: {{ $('Rendered OK?').item.json.error }}\n\nThe row is now 'needs_fix'. Ask the Quartet Songs project to correct the chords JSON and paste the new SQL; that sets it back to 'ready'.",
        "options": {
          "appendAttribution": false
        }
      },
      "id": "email-the-error",
      "name": "Email the error",
      "type": "n8n-nodes-base.gmail",
      "typeVersion": 2.1,
      "position": [
        1340,
        600
      ]
    }
  ],
  "connections": {
    "Every 5 minutes": {
      "main": [
        [
          {
            "node": "Settings",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Settings": {
      "main": [
        [
          {
            "node": "Songs ready for clips",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Songs ready for clips": {
      "main": [
        [
          {
            "node": "Render chord clips",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Render chord clips": {
      "main": [
        [
          {
            "node": "Rendered OK?",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Rendered OK?": {
      "main": [
        [
          {
            "node": "Upload reference clip",
            "type": "main",
            "index": 0
          },
          {
            "node": "Upload tag clip",
            "type": "main",
            "index": 0
          },
          {
            "node": "Email the song",
            "type": "main",
            "index": 0
          }
        ],
        [
          {
            "node": "Mark needs fix",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Email the song": {
      "main": [
        [
          {
            "node": "Mark clips ready",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Mark needs fix": {
      "main": [
        [
          {
            "node": "Email the error",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "active": false
}
